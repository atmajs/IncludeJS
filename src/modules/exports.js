var __nativeRequire,
	__includeRequire;

if (global.require) {
	var name = global.require.name;
	if (name !== 'amd' && name !== 'commonjs') {
		__nativeRequire = global.require;
	}
}

// import ./common.js
// import ./amd.js
