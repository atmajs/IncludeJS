UTest({

	$before () {
		include = includeModule
			.include
			.instance('/test/use.node.test');
	},

	'should load script with `use` feature': function(done){
		include
			.js(
				'use/a.js::a_res',
				'use/c.js::c',
				'use/b.js'
			)
			.done(function(resp){
				eq(resp.b, 'ab');
				done();
			});
	}
});
